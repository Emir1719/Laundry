import 'package:flutter/material.dart';

class BtnMachine extends StatelessWidget {
  const BtnMachine({super.key});

  @override
  Widget build(BuildContext context) {
    return IconButton(
      icon: const Icon(Icons.add),
      iconSize: 35,
      color: Colors.black,
      onPressed: () {},
    );
  }
}
